# LeiloAI - Cron Monitor: pinga health dos 18 workers Cloudflare a cada 15 min.
# Abre GitHub issue (label "incident") se algum worker retornar 5xx ou timeout.
# Nunca derruba CI: failure de worker so abre issue, job sai sucesso.

name: "LeiloAI: Cron Monitor"

on:
  schedule:
    - cron: '*/15 * * * *' # a cada 15 minutos
  workflow_dispatch:

permissions:
  issues: write
  contents: read

concurrency:
  group: leiloai-cron-monitor
  cancel-in-progress: false

jobs:
  health_check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        worker:
          - { slug: ingest-bb,             url: 'https://leiloai-ingest-bb.conta-removida-da-historia.invalid' }
          - { slug: ingest-caixa,          url: 'https://leiloai-ingest-caixa.conta-removida-da-historia.invalid' }
          - { slug: w05,    url: 'https://leiloai-w05.conta-removida-da-historia.invalid' }
          - { slug: w08,        url: 'https://leiloai-w08.conta-removida-da-historia.invalid' }
          - { slug: ingest-detran,         url: 'https://leiloai-ingest-detran.conta-removida-da-historia.invalid' }
          - { slug: w12,           url: 'https://leiloai-w12.conta-removida-da-historia.invalid' }
          - { slug: w13,            url: 'https://leiloai-w13.conta-removida-da-historia.invalid' }
          - { slug: ingest-dou,            url: 'https://leiloai-ingest-dou.conta-removida-da-historia.invalid' }
          - { slug: ingest-marketplaces,   url: 'https://leiloai-w02.conta-removida-da-historia.invalid' }
          - { slug: w01, url: 'https://leiloai-w01.conta-removida-da-historia.invalid' }
          - { slug: ingest-receita,        url: 'https://leiloai-ingestao-receita.conta-removida-da-historia.invalid' }
          - { slug: ingest-rental,         url: 'https://leiloai-w07.conta-removida-da-historia.invalid' }
          - { slug: ingest-seuimovelbb,    url: 'https://leiloai-ingest-seuimovelbb.conta-removida-da-historia.invalid' }
          - { slug: ingest-spyleiloes,     url: 'https://leiloai-ingest-spyleiloes.conta-removida-da-historia.invalid' }
          - { slug: ingest-bancos,         url: 'https://leiloai-w06.conta-removida-da-historia.invalid' }
          - { slug: wc1,            url: 'https://leiloai-wc1.conta-removida-da-historia.invalid' }
          - { slug: w09,         url: 'https://leiloai-w09.conta-removida-da-historia.invalid' }
          - { slug: w11,          url: 'https://leiloai-w11.conta-removida-da-historia.invalid' }
    steps:
      - name: Pinga ${{ matrix.worker.slug }}
        id: ping
        continue-on-error: true
        run: |
          set +e
          # /health quando existir; fallback para / na raiz
          CODE=$(curl -sS --max-time 15 -o /tmp/body.txt -w '%{http_code}' \
            "${{ matrix.worker.url }}/health" || echo "000")
          if [ "$CODE" = "404" ]; then
            CODE=$(curl -sS --max-time 15 -o /tmp/body.txt -w '%{http_code}' \
              "${{ matrix.worker.url }}/" || echo "000")
          fi
          echo "code=$CODE" >> "$GITHUB_OUTPUT"
          BODY=$(head -c 400 /tmp/body.txt 2>/dev/null || echo "")
          {
            echo "body<<EOF"
            echo "$BODY"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          echo "[$CODE] ${{ matrix.worker.url }}"
          # 000 = curl falhou (timeout/DNS), 5xx = erro do worker
          if [ "$CODE" = "000" ] || { [ "$CODE" -ge 500 ] 2>/dev/null && [ "$CODE" -lt 600 ] 2>/dev/null; }; then
            echo "incident=true" >> "$GITHUB_OUTPUT"
          else
            echo "incident=false" >> "$GITHUB_OUTPUT"
          fi
          # Sempre exit 0 para nao derrubar CI
          exit 0

      - name: Abre issue se incidente em ${{ matrix.worker.slug }}
        if: steps.ping.outputs.incident == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const dia = new Date().toISOString().slice(0, 10);
            const slug = '${{ matrix.worker.slug }}';
            const url = '${{ matrix.worker.url }}';
            const code = '${{ steps.ping.outputs.code }}';
            const body = `${{ steps.ping.outputs.body }}`;
            const titulo = `[incident] worker ${slug} indisponivel ${dia}`;
            const corpo = [
              `Worker: \`${slug}\``,
              `URL: ${url}/health`,
              `Status: ${code}`,
              ``,
              `Resposta:`,
              `\`\`\``,
              body || '(vazio)',
              `\`\`\``,
              ``,
              `Detectado em: ${new Date().toISOString()}`,
              `Workflow: cron-monitor matrix`,
            ].join('\n');
            const { data: existentes } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              labels: 'incident',
              state: 'open',
              per_page: 100,
            });
            // Dedup: 1 issue por worker por dia
            if (existentes.find(i => i.title === titulo)) {
              console.log('Issue ja existe para hoje, ignorando.');
              return;
            }
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: titulo,
              body: corpo,
              labels: ['incident', 'leiloai-monitor'],
            });
